hostname EdgeRouter-0

interface GigabitEthernet0/0
    ip address 10.0.0.18 255.255.255.252
    ip nat inside
    ip ospf network point-to-point

interface GigabitEthernet0/1
    ip address dhcp
    ip nat outside

interface GigabitEthernet0/2 
    ip address dhcp
    ip nat outside

interface loopback 0
    ip address 5.5.5.5 255.255.255.255

router ospf 5 
    network 10.0.0.16 0.0.0.3 area 0
    router-id 5.5.5.5
    default-information originate always metric 10

interface GigabitEthernet0/0
    ip address 10.0.0.18 255.255.255.252
    ip ospf network point-to-point

ip sla 1
    icmp-echo 8.8.8.8 source-interface GigabitEthernet0/1
    frequency 10
    timeout 5000
    threshold 5000

ip sla schedule 1 life forever start-time now

track 1 ip sla 1 reachability

ip route 0.0.0.0 0.0.0.0 203.0.113.1 track 1
ip route 0.0.0.0 0.0.0.0 198.51.100.1 10

ip access-list standard VLAN_NAT
1 permit 192.168.0.0 0.0.0.255

route-map NAT_ISP-0 permit 10
    match ip address VLAN_NAT
    match interface GigabitEthernet0/1

route-map NAT_ISP-1 permit 10
    match ip address VLAN_NAT
    match interface GigabitEthernet0/2

ip nat inside source route-map NAT_ISP-0 interface GigabitEthernet0/1 overload
ip nat inside source route-map NAT_ISP-1 interface GigabitEthernet0/2 overload

ip domain-lookup
ip name-server 192.168.0.132
ip domain-name lablocal.pl

enable secret lab123!_

service password-encryption

username admin secret lab123!_

crypto key generate rsa 2048
ip ssh version 2

line vty 0 15
    exec-timeout 5
    transport input ssh
    login local
    access-class SSH_ACCESS in

ip access-list standard SSH_ACCESS
    permit 192.168.1.0 0.0.0.31
    deny any

line console 0
    exec-timeout 5
    password lab123!_
    login